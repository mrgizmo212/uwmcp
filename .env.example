# Upstream base URL (set to your deployed proxy)
# Example: https://uwapi.onrender.com
UW_BASE_URL=https://uwapi.onrender.com

# Authentication
# For uwmcp → uwapi: set only the UW_API_KEY (X-API-Key for your proxy)
UW_API_KEY=customkey212
# Leave UW_BEARER_TOKEN empty in uwmcp. Set it in uwapi/.env so uwapi can auth upstream.
# UW_BEARER_TOKEN=

# Networking
UW_TIMEOUT_SECONDS=120

# Server runtime
# Used by uwmcp/run_http.py when running streamable-http
PORT=8000


